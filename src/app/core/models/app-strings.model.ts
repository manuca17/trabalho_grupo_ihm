/**
 * Collects translatable application strings loaded from JSON.
 */
export interface AppStrings {
  tabLabels: {
    inventory: string;
    addOffer: string;
    negotiations: string;
  };
  headings: {
    inventory: string;
    addOffer: string;
    negotiations: string;
  };
  cta: {
    viewDetails: string;
    publishOffer: string;
    openNegotiation: string;
    markAsTraded: string;
  };
}
